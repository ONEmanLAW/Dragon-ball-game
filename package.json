{
  "name": "dragon-ball-tactical-clash",
  "version": "1.0.0",
  "type": "module",
  "main": "dist/electron/main.js",
  "scripts": {
    "dev": "vite --open",
    "build": "vite build",
    "build:electron": "tsc -p electron/tsconfig.json",
    "game": "npm run build && npm run build:electron && electron ."
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "electron": "^31.0.0",
    "typescript": "^5.0.0",
    "vite": "^5.0.0"
  }
}
