// ───────────────────────────────
// Entry — bootstrap UI
// ───────────────────────────────

import { bootGameUI } from "./ui/GameUi";

document.addEventListener("DOMContentLoaded", () => {
  bootGameUI();
});
